package pack05_Android;

public class Button {
	//nested Interface
	public OnClickListner listener;
	
	//중첩 인터페이스를 사용
	//interface가 최상위가 아니라 클래스 내부에 interface를 구현해놓음
	static interface OnClickListner() {
		void onClick();
	}
}
