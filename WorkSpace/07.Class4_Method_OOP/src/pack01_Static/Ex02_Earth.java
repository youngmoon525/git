package pack01_Static;

public class Ex02_Earth {
	static final double PI = 3.14;
	static final double EARTH_RADIUS = 6400;
	static final double EARTH_AREA = 4 * PI * EARTH_RADIUS * EARTH_RADIUS;
	//static final double EARTH_AREA2 = 4 * Math.PI * EARTH_RADIUS * EARTH_RADIUS;
	//1.final 상수인 지구 표면적을 변수인 double EARTH_AREA 
	//지구의 표면적을 구하는 식은 4 * pi * 지구 반지름 * 지구 반지름
	//해당하는 표면적값을 EarthMain에서 출력하시오
	//단 ※ Ex02_Earth는 인스턴스화 하지 않는다.
	//		그리고 지구의 반지름 값과 표면적 값은 변하지 않는 수이다.
	
	//절대 변하지 않는 값은 final이란 키워드로 상수라고 한다.
	//final 변수의(상수) 이름 규칙은 대문자로 전부 작성
}
